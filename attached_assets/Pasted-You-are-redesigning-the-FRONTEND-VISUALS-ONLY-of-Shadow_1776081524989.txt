You are redesigning the FRONTEND VISUALS ONLY of ShadowPay — a private Solana 
payment links app. DO NOT touch any blockchain logic, wallet connection code, 
escrow programs, stealth address derivation, transaction signing, or any backend 
functionality. Only change CSS, layout, animations, and visual presentation.

===================================================================
GOAL: Make ShadowPay look like a AAA crypto product — dark, 3D, 
immersive, cinematic. Think Phantom Wallet meets Stripe meets a 
hacker movie UI.
===================================================================

---

## INSTALL THESE FRONTEND PACKAGES ONLY
- framer-motion
- three
- @react-three/fiber
- @react-three/drei
- react-countup
- react-hot-toast (replace any existing toasts with this)

---

## FONTS
Replace current fonts with:
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

- All UI text → Space Grotesk
- Wallet addresses, hashes, numbers → JetBrains Mono

---

## COLOR SYSTEM (replace ALL existing colors)
:root {
  --bg-void:       #03030A;   /* deepest background */
  --bg-surface:    #0A0A1A;   /* card backgrounds */
  --bg-elevated:   #0F0F25;   /* elevated cards */
  --border:        rgba(124, 58, 237, 0.15);
  --border-hover:  rgba(124, 58, 237, 0.4);
  --purple:        #7C3AED;
  --purple-light:  #A855F7;
  --purple-glow:   rgba(124, 58, 237, 0.3);
  --cyan:          #06B6D4;
  --cyan-glow:     rgba(6, 182, 212, 0.25);
  --gold:          #F59E0B;
  --green:         #10B981;
  --green-glow:    rgba(16, 185, 129, 0.3);
  --text-primary:  #F0F0FF;
  --text-secondary:#8B8BAD;
  --text-muted:    #4A4A6A;
}

---

## 3D ANIMATED BACKGROUND (add as a fixed canvas behind everything)

Create a file: src/components/CryptoBackground.jsx

Use @react-three/fiber + @react-three/drei:

1. PARTICLE FIELD
   - 4000 particles, randomly placed in a sphere of radius 80
   - Colors: mix of #7C3AED, #06B6D4, #ffffff at varying opacity
   - Sizes: random between 0.02 and 0.08
   - Slowly rotate the entire field on Y axis (0.0003 per frame)
   - Subtle mouse parallax: camera shifts slightly toward cursor

2. FLOATING GEOMETRIC SHAPES
   - 1x wireframe IcosahedronGeometry (radius 8) — purple, slowly 
     rotating on all 3 axes, positioned top-right area
   - 1x wireframe TorusKnotGeometry (radius 4, tube 1.2) — cyan, 
     slowly rotating, positioned bottom-left
   - 1x wireframe OctahedronGeometry (radius 5) — gold, slowly 
     rotating, positioned center-left
   - All at 15–20% opacity, they should feel like background ghosts

3. GLOW ORBS (use Sprite or PointLight)
   - 3 large soft glow blobs: purple (top-left), cyan (bottom-right), 
     gold (center) — they pulse scale slowly with a sine wave

Mount this canvas as position: fixed, inset: 0, z-index: 0
Wrap everything else in z-index: 1 relative container

---

## GLOBAL STYLES

body {
  background: var(--bg-void);
  min-height: 100vh;
  overflow-x: hidden;
}

/* Glass morphism card mixin — apply to ALL cards */
.glass-card {
  background: rgba(10, 10, 26, 0.7);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--border);
  border-radius: 20px;
  transition: border-color 0.3s, box-shadow 0.3s, transform 0.3s;
}
.glass-card:hover {
  border-color: var(--border-hover);
  box-shadow: 0 0 40px var(--purple-glow), 0 20px 60px rgba(0,0,0,0.5);
  transform: translateY(-2px);
}

/* 3D tilt on hover — apply to payment card */
.tilt-card {
  transform-style: preserve-3d;
  transition: transform 0.15s ease;
}

/* Neon text glow */
.glow-purple { text-shadow: 0 0 20px var(--purple), 0 0 40px var(--purple-glow); }
.glow-cyan   { text-shadow: 0 0 20px var(--cyan),   0 0 40px var(--cyan-glow); }
.glow-gold   { text-shadow: 0 0 20px var(--gold); }

/* Shimmer animation */
@keyframes shimmer {
  0%   { background-position: -200% center; }
  100% { background-position:  200% center; }
}
.shimmer-text {
  background: linear-gradient(90deg, var(--purple-light), var(--cyan), var(--purple-light));
  background-size: 200% auto;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: shimmer 3s linear infinite;
}

/* Floating bob animation for decorative elements */
@keyframes float {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  33%       { transform: translateY(-12px) rotate(2deg); }
  66%       { transform: translateY(-6px) rotate(-1deg); }
}

/* Pulse glow for live indicator */
@keyframes pulse-green {
  0%, 100% { box-shadow: 0 0 0 0 var(--green-glow); }
  50%       { box-shadow: 0 0 0 8px transparent; }
}

/* Gradient border animation */
@keyframes border-spin {
  to { --angle: 360deg; }
}

---

## NAVBAR REDESIGN

Layout: fixed top, full width, z-index: 100
Background: rgba(3, 3, 10, 0.8) with backdrop-filter: blur(30px)
Bottom border: 1px solid var(--border)

LEFT — Logo:
  - 🎩 emoji or shadow-hat SVG icon with purple drop-shadow
  - "Shadow" in white bold + "Pay" in var(--purple) bold
  - Subtle purple glow behind the logo icon

CENTER — Live Ticker Marquee:
  - Infinite scrolling left ticker strip
  - Green pulsing dot "● LIVE" then pipe-separated stats
  - Text in JetBrains Mono, 12px, var(--text-secondary)
  - Positive values (+2.4%) in var(--green)
  - Wrap in overflow: hidden container with CSS animation scroll

RIGHT:
  - "My Payments" button: ghost style, purple border, purple text
    hover: fills with purple gradient
  - Wallet pill: JetBrains Mono font, gradient background 
    (purple → cyan), rounded-full, subtle glow

---

## HERO SECTION REDESIGN

Centered, padding: 80px 20px 40px

HEADING — three lines, massive font (clamp 48px → 80px):
  Line 1: "Private" — white, font-weight: 800
  Line 2: "Payment" — shimmer-text class (purple→cyan gradient animated)
  Line 3: "Links" — var(--purple), font-weight: 800, glow-purple class

Animate with Framer Motion:
  - Each word: initial={{ opacity:0, y:40 }}, animate={{ opacity:1, y:0 }}
  - Stagger delay: 0.15s between words

SUBTEXT:
  - Color: var(--text-secondary)
  - Max-width: 420px, centered

STAT PILLS ROW (3 pills, glass-card style):
  Apply .glass-card to each pill
  Each pill has:
    - Small icon (colored accordingly)
    - Label above in var(--text-muted), 11px caps
    - Value in white, JetBrains Mono, 15px
  BALANCE pill: icon pulses cyan
  NETWORK pill: green dot blinks with pulse-green animation
  TX FEE pill: gold colored value

---

## MAIN PAYMENT CARD REDESIGN

Width: 480px max, centered
Apply .glass-card + .tilt-card classes

Add JS mousemove listener on the card:
  card.style.transform = `
    perspective(1000px)
    rotateX(${-deltaY * 0.01}deg)
    rotateY(${deltaX * 0.01}deg)
  `
  Reset on mouseleave

Add a subtle animated gradient border:
  Use a pseudo-element or conic-gradient border that slowly 
  rotates — purple to cyan to purple

TABS (Send / Receive):
  - Active tab: gradient background (purple → cyan), white text, 
    rounded-xl, subtle glow below
  - Inactive: transparent, var(--text-muted)
  - Animated slide indicator underline

INFO BANNER:
  - Background: rgba(124, 58, 237, 0.08)
  - Border-left: 3px solid var(--purple)
  - Green pulsing dot + italic text

TOKEN SELECTOR:
  Each token button (.glass-card):
    - Token icon: colored circle SVG (purple S for SOL, cyan $ for USDC)
    - Token name bold + subname muted below
    - ACTIVE state: border-color var(--purple), box-shadow 0 0 20px 
      var(--purple-glow), checkmark badge in top-right corner with 
      green fill + scale-in animation

AMOUNT INPUT:
  - Background: rgba(255,255,255,0.03)
  - Border: 1px solid var(--border)
  - Font: JetBrains Mono, 32px, white
  - "SOL" / "USDC" label: var(--purple), right side
  - On focus: border glows purple, faint purple background tint
  - Fee row text: JetBrains Mono, 11px, var(--text-muted)

NOTE INPUT:
  - Same glass style
  - Placeholder styled with var(--text-muted), italic

WALLET ROW:
  - Glass pill, green glow border
  - Green pulsing dot
  - Address in JetBrains Mono
  - "Connected" badge in var(--green), tiny font

PRIMARY CTA BUTTON:
  - Full width, height: 56px, border-radius: 14px
  - Background: linear-gradient(135deg, #7C3AED, #06B6D4)
  - Font: Space Grotesk, 16px, 600 weight, white
  - Default box-shadow: 0 4px 30px rgba(124,58,237,0.4)
  - HOVER:
    · scale(1.02)
    · box-shadow: 0 8px 50px rgba(124,58,237,0.7)
    · Shimmer sweep: white diagonal highlight animates left→right 
      across button in 0.6s
  - ACTIVE: scale(0.99)
  - LOADING state: replace text with spinning ring + "Processing..."
  - All transitions: 0.2s ease

---

## FLOATING DECORATIVE ELEMENTS (CSS only, no interaction)

Add 6 faint Solana "S" logo SVGs scattered around the viewport:
  - Sizes: 24px, 32px, 40px (mix)
  - Opacity: 0.06 to 0.12
  - Each has float animation with different duration (8s, 11s, 14s, 
    9s, 13s, 10s) and different delay
  - Colors: purple, cyan, white (mix)
  - Position: fixed, scattered at edges — don't overlap main card

---

## SUCCESS MODAL (shown after send button click)

Full screen overlay:
  - Background: rgba(3,3,10,0.85)
  - backdrop-filter: blur(30px)
  - Framer Motion: fade in

Modal card (.glass-card):
  - Width: 420px, centered
  - Animated gradient border (purple→cyan, rotating)
  - Border-radius: 24px

Content:
  1. Animated checkmark SVG (stroke-dasharray draw animation, green)
     surrounded by green glow pulse ring
  2. "Link Generated!" — Space Grotesk 28px bold, shimmer-text
  3. Payment link in glass pill: JetBrains Mono 13px
     "shadowpay.replit.app/claim#a7f3...e92b"
  4. Two buttons side by side:
     - "Copy Link" — ghost purple button with copy icon
     - "Share" — ghost cyan button with share icon
     On copy: button flashes green + "Copied!" for 2s
  5. Caption row: 🔒 "Stealth address created · Wallet hidden on-chain"
     in var(--green), 12px

Close: X button top-right, hover rotates 90deg

---

## HOW IT WORKS SECTION

Title: "Three Steps to Total Privacy" — white h2, centered
Subtitle: var(--text-secondary)

3 cards in a row (stack on mobile), .glass-card:
  Card 01 — "Generate Link" — gold number
  Card 02 — "Share the Link" — purple number  
  Card 03 — "Claim Privately" — cyan number

Each card:
  - Large step number: 80px, bold, 8% opacity as watermark behind content
  - Icon: relevant emoji or lucide icon, 32px, colored
  - Icon has float animation
  - Title: white, 18px, 600
  - Description: var(--text-secondary), 14px
  - Bottom badge: small colored pill with key phrase
    ("Sender funds escrow" / "No address in URL" / "Wallet never revealed")
  - Hover: translateY(-8px) + glow intensifies

Arrows between cards: cyan colored, fade in with delay

---

## WHY SOLANA STATS SECTION

3 metric cards (.glass-card) in a row:

Card 1 — SPEED:
  - "400" in var(--purple), 72px, JetBrains Mono, glow-purple
  - "ms" in var(--text-secondary)  
  - "block time" label
  - "Claim + sweep in under 2 seconds" subtext

Card 2 — COST (highlighted, slightly larger):
  - "$0.001" in var(--gold), 72px, JetBrains Mono, glow-gold
  - "per transaction" label
  - "Cheaper than a text message" subtext
  - Extra glow: gold box-shadow

Card 3 — TOKENS:
  - SOL + USDC icons side by side, 40px each
  - "SOL + USDC" label, var(--cyan)
  - "non-custodial" subtext
  - "Your keys. Your funds." in var(--green), small

All numbers animate in with react-countup when scrolled into view
Use IntersectionObserver to trigger

---

## FOOTER

Background: rgba(3,3,10,0.9), border-top: 1px solid var(--border)
Padding: 40px

Left: ShadowPay logo + "Private payment links on Solana"
Center: "shadowpay.replit.app" in var(--purple), JetBrains Mono
Right: SOL + USDC token icons + "Mainnet" green badge

Bottom row: "Built on Solana · Powered by Stealth Addresses · 
Non-custodial · No data collected" — var(--text-muted), 12px, centered

---

## RESPONSIVE
- Below 768px: all multi-column grids become single column
- Main card: full width with 16px margin
- Hero font: clamp(36px, 8vw, 80px)
- Navbar: hide ticker, show hamburger (no menu needed, just hide ticker)
- Floating decorative elements: hide on mobile

---

## FINAL CHECKLIST
✅ DO touch: all CSS, colors, fonts, animations, layout, card styles
✅ DO touch: adding Three.js canvas, Framer Motion, particle field
✅ DO touch: button hover effects, modal design, transitions
❌ DO NOT touch: wallet adapter code, any Solana web3.js logic
❌ DO NOT touch: escrow transaction functions, stealth key derivation  
❌ DO NOT touch: any API calls, program IDLs, anchor configs
❌ DO NOT touch: environment variables, RPC endpoints
❌ DO NOT touch: any file that handles signing or sending transactions